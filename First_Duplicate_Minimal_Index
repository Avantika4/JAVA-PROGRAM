int firstDuplicate(int[] a) {
    HashSet<Integer> x= new HashSet<>();
    for(int i=0;i<a.length;i++)
    {
        int b=a[i];
        if(x.contains(b))return b;
        x.add(b);
    }
    return -1;

}
